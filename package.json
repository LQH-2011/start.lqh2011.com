{
  "name": "start-lqh2011-sync-api",
  "private": true,
  "version": "1.0.0",
  "description": "Sync backend for start.lqh2011.com — Vercel serverless functions + Neon Postgres",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "hash": "node scripts/hash-password.js",
    "check": "node scripts/check-inline-js.js",
    "test": "npm run check && npm run test:api && npm run test:ui",
    "test:api": "node --test \"tests/api/**/*.test.js\"",
    "test:ui": "playwright test"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
